/** @type {import('ts-jest').JestConfigWithTsJest} **/
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  transform: {
    '^.+\\.tsx?$': [
      'ts-jest',
      {
        useESM: true,
      },
    ],
  },
  extensionsToTreatAsEsm: ['.ts'],
  moduleNameMapper: {
    '^(\\.{1,2}/.*)\\.js$': '$1',
  },
  // Specify which files Jest should run as tests
  testMatch: ['<rootDir>/src/**/*.test.ts'],
  // Explicitly exclude the dist directory
  testPathIgnorePatterns: ['/node_modules/', '/dist/'],
};
